"""
Test apps configuration.
Edit this file to configure client.py and resource_as.py.
"""

CONFIG = {

    # IdP base URL — shared by client.py and resource_as.py
    'idp_url': 'https://idp.example.com',

    # Resource AS URL — shared by client.py (sent as audience) and resource_as.py (verifies aud claim)
    'resource_as_url': 'https://auth.resource.example.com',

    # ── client.py (port 5001) ─────────────────────────────────────────────────

    'client': {

        # Application credentials — get these from the IdP after registering
        'client_id':     '[CHANGE ME]',
        'client_secret': '[CHANGE ME TOO!]',

        # IdP endpoints — change these when connecting to a third-party server
        #'idp_auth_endpoint':        'https://idp.example.com/authorize',
        #'idp_token_endpoint':       'https://idp.example.com/token',
        #'idp_end_session_endpoint': '',   # OIDC RP-Initiated Logout (RFC 9177)

        # Resource endpoint to call
        #'resource': 'https://api.resource.example.com/api/todos',

        # Resource AS token endpoint
        #'resource_token_endpoint': 'https://auth.resource.example.com/token',

        # Flask session signing key
        'client_secret_key': 'client-dev-secret-key',
    },

    # ── resource_as.py (port 5002) ────────────────────────────────────────────

    'resource_as': {
        # No resource_as-specific settings at this time.
    },
}
